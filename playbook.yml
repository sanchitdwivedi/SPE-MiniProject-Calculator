---
- hosts: dev
  become: true
  tasks:
    - name: Start the container
      docker_container:
        name: calculator
        image: "sanchitdwivedi/calculator:{{DOCKER_VERSION}}"
        state: started
        published_ports:
          - 0.0.0.0:8080:8080
